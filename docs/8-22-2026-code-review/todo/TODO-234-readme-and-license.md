# TODO-234 — README.md (what/where/how) and a LICENSE decision

**Priority:** P1   **Effort:** S   **Theme:** C — presentation/platform/maintainability
**Source findings:** 06-architecture § 10.2 README outline, § 8.7 (no README/LICENSE), F-13; 04-security LOW-11 (public repo, legal default "all rights reserved")
**Depends on:** TODO-211 (commands it documents)   **Blocks:** none

## Why
The repository is public, has 45 commits and zero README/LICENSE. A newcomer (or a future Claude session) cannot learn the live URL, how to build, that `index.html` is generated, or whether they may reuse the code. Without a LICENSE file the legal default is all-rights-reserved, which is probably not the intent for a student project that already credits Claude in 29 commits.

## Current behaviour (verified)
- Root: `.gitignore JpnFlashcards.jsx cf data docs index.html netlify netlify.toml tools` — no `README.md`, no `LICENSE`.
- Only runbook-style doc: `tools/REFRESH-VIDEO-INDEX.md` (81 lines; commands, expected numbers, Claude prompt, known weakness).
- Remote: `https://github.com/matthewames/tangocho.git`; live app `https://tangocho.deskbuddies.workers.dev`; short URL worker `jp.deskbuddies.workers.dev` (`cf/short/`).
- Build: `cd tools && npm install && node build.mjs` (→ `npm run build` after TODO-211); deploy `cd cf && npx wrangler deploy`.

## Intended behaviour
`README.md` (~120 lines) and `LICENSE` at the root; the README links ARCHITECTURE/DATA-SCHEMA/CONTRIBUTING/RUNBOOK (TODO-235/236/237).

## Implementation steps
1. Write `README.md` with these sections (fill from the source; no marketing):
   1. **tangocho (単語帳)** — one paragraph: a single-file React app for Matthew's JPN 101 (NihonGO NOW!) vocabulary with FSRS scheduling, kana handwriting drills, dialogue rehearsal with TTS, conjugation drills, a "10k"/core-words deck and a comprehensible-input recommender; built with Claude Code. Screenshot or the nigiri GIF (`data/mascot.js` → add a `docs/img/home-375.png`).
   2. **Live**: app URL, short URL; what is stored where (localStorage on each device ⇄ Cloudflare KV per Google account; no accounts required to study locally).
   3. **Quick start**: `nvm use` (`.nvmrc`), `npm ci`, `npm run build`, `npm test`, `npm run dev` (wrangler dev on :8787, needs `cf/.dev.vars` — link CONTRIBUTING), `npm run deploy`.
   4. **Repo map** (tree: `src/` after modularization or `JpnFlashcards.jsx` today, `tools/`, `cf/`, `data/`, `public/`, `test/`, `e2e/`, `docs/`) — one line each, marking generated files: `index.html` (build output, committed), `cf/public/` (gitignored), `data/videos.json` (generated by `tools/yt-*.mjs`), `src/data/mascot.js` (generated by `tools/make-mascot.mjs`).
   5. **Deploy**: CI deploys `main` (TODO-213) or `npm run deploy`; link RUNBOOK for rollback/secrets.
   6. **Data refresh tasks**: add words (SEED_VERSION bump steps → CONTRIBUTING), refresh video index (`tools/REFRESH-VIDEO-INDEX.md`), regenerate mascot/icons.
   7. **Status/CI badge** (`![ci](https://github.com/matthewames/tangocho/actions/workflows/ci.yml/badge.svg)`).
   8. **License**.
2. **LICENSE decision** (Matthew's call; present the options in the PR description):
   - *MIT* — simplest, lets classmates fork; code only.
   - *Content caveat*: `SEED`/`SCRIPT_SEED` are Matthew's course notes derived from a commercial textbook (NihonGO NOW!); state in README that the vocabulary/scripts data is provided for personal study and is **not** relicensed, e.g. "Code: MIT. Vocabulary and script data: © Matthew Ames, all rights reserved; derived from class materials — do not redistribute." Alternatively *CC BY-NC 4.0* for the data if sharing with classmates is intended.
   - Third-party notices: React (MIT) is bundled into `index.html` (its license comments survive minification — see the tail of `index.html`); `tools/gifenc.mjs` is hand-written; `data/videos.json` contains only titles/ids.
   Write `LICENSE` with the chosen text (MIT template, year 2026, "Matthew Ames") and the data notice in README.
3. Link the five docs from README (create placeholders if those TODOs land later).

4. **README skeleton** (paste and fill):
   ```markdown
   # tangocho 単語帳
   ![ci](https://github.com/matthewames/tangocho/actions/workflows/ci.yml/badge.svg)
   One-file Japanese study app for JPN 101 (NihonGO NOW!) — FSRS flashcards, kana handwriting, dialogue rehearsal with TTS, conjugation drills, a core-words deck, and a comprehensible-input recommender. Built by Matthew with Claude Code.
   **Live:** https://tangocho.deskbuddies.workers.dev (short: https://jp.deskbuddies.workers.dev)
   ## How it stores your progress
   … localStorage on each device; optional Google sign-in syncs to Cloudflare KV; Backup/Restore JSON in Browse › More …
   ## Develop
   ```sh
   nvm use && npm ci
   npm run build      # regenerates index.html + cf/public/  (commit index.html with your change!)
   npm test           # node --test test/ cf/test/
   npm run dev        # wrangler dev on http://localhost:8787 (needs cf/.dev.vars — see CONTRIBUTING.md)
   npm run deploy     # or let CI deploy main
   ```
   ## Repo map
   | path | what | generated? |
   |---|---|---|
   | `src/` (today: `JpnFlashcards.jsx`) | the app | |
   | `index.html` | the deployable single file | **yes — never edit by hand** |
   | `cf/` | Cloudflare Worker (assets + /api/sync, /api/tts, /api/feed) and `cf/short` redirector | `cf/public/` yes |
   | `tools/` | build, checks, mascot/icon generators, YouTube index pipeline | |
   | `data/videos.json` | indexed video library | yes (`tools/REFRESH-VIDEO-INDEX.md`) |
   | `public/` | manifest, icons, sw.js template | |
   | `test/`, `e2e/` | unit + Playwright | |
   | `docs/` | reviews, ARCHITECTURE, DATA-SCHEMA, RUNBOOK | |
   ## Docs
   ARCHITECTURE.md · DATA-SCHEMA.md · CONTRIBUTING.md · RUNBOOK.md · CLAUDE.md · docs/8-22-2026-code-review/
   ## License
   Code: MIT (see LICENSE). Vocabulary/script data: © Matthew Ames — personal study use; not for redistribution.
   ```

## Data migration / compatibility
none

## Testing & verification
- `npx markdownlint README.md` (optional) or just read it on GitHub; every command in Quick start runs on a fresh clone (`git clone … /tmp/t && cd /tmp/t && npm ci && npm run build && npm test`).
- GitHub shows the license badge after `LICENSE` is committed.

## Acceptance criteria
- [ ] README covers the 8 sections; commands verified on a fresh clone.
- [ ] LICENSE committed with an explicit decision on code vs data.
- [ ] Generated files clearly marked; links to the other docs.

## Pitfalls / notes
- Don't paste secrets or the Google client id into the README beyond what is already in source (the client id is public by design, per report 04).
- Keep the README short; architecture details go to ARCHITECTURE.md (TODO-235).
