{
  "name": "tangocho-build-tools",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling for tangocho. Kept out of the repo root on purpose so Netlify keeps doing zero build steps and just serves the committed index.html.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
